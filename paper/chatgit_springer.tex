% ============================================================
% ChatGIT: Granularity-Adaptive Conversational Repository Intelligence
% Springer LNCS — ready for ECIR / CIKM / SIGIR submission
%
% Authors: Anish Gupta, Prakhar Sethi, Ritwik Bhattacharya,
%          Dr. Sonia Khetarpaul
% Institution: SNU — School of Engineering and Applied Science
%
% CHANGE LOG (all from research_progress.md audit):
%  v3 → v4 (current):
%   - Removed Setting A (200-query) — no source annotation file exists
%   - Renamed RepoCoder baseline → BM25-SlidingWindow (honest labelling)
%   - Added ConvAwareRAG baseline to isolate N3 contribution
%   - Added N3-only and N4-only ablation rows
%   - Fixed SUMMARIZE ground-truth (module_summary chunks now in GT)
%   - Added Dataset Construction subsection (transparent about auto-gen)
%   - Expanded Click error analysis
%   - Strengthened Limitations and Discussion
%   - Added latency/efficiency section
%   - Updated all tables with full-benchmark numbers
% ============================================================

\documentclass[runningheads]{llncs}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{hyperref}
\usepackage{multirow}
\usepackage{array}
\usepackage{xspace}
\usepackage[inline]{enumitem}
\usepackage{microtype}
\usepackage[table]{xcolor}

\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, backgrounds, calc, fit}

% ── Colours ───────────────────────────────────────────────
\definecolor{cBlue}  {HTML}{3B82F6}
\definecolor{cIndigo}{HTML}{6366F1}
\definecolor{cPurple}{HTML}{A855F7}
\definecolor{cGreen} {HTML}{16A34A}
\definecolor{cDark}  {HTML}{1E293B}
\definecolor{cAmber} {HTML}{D97706}

% ── Macros ────────────────────────────────────────────────
\newcommand{\chatgit}{\textsc{ChatGIT}\xspace}
\newcommand{\conv}{\textsc{ConvCodeBench}\xspace}

% ══════════════════════════════════════════════════════════
\begin{document}

\title{ChatGIT: Granularity-Adaptive Conversational Repository
Intelligence with Multi-Turn Session Memory, Graph-Structural
Ranking, and Bidirectional Call-Context Augmentation}

\titlerunning{ChatGIT: Conversational Repository Intelligence}

\author{Anish Gupta\inst{1} \and
        Prakhar Sethi\inst{1} \and
        Ritwik Bhattacharya\inst{1} \and
        Sonia Khetarpaul\inst{1}}

\authorrunning{Gupta, Sethi, Bhattacharya, Khetarpaul}

\institute{
  Computer Science and Engineering,
  School of Engineering and Applied Science,
  Shiv Nadar University (SNU), India\\
  \email{\{ag801, ps385, rb213\}@snu.edu.in,\;
         sonia.khetarpaul@snu.edu.in}
}

\maketitle

% ══════════════════════════════════════════════════════════
\begin{abstract}
Navigating a large, unfamiliar codebase through conversational
question-answering is fundamentally different from single-turn
code search: the retrieval system must suppress already-seen
content, track topical focus across turns, and adapt context
depth to query intent. We present \chatgit, a Retrieval-Augmented
Generation (RAG) system for multi-turn conversational QA over
GitHub repositories. Built on five complementary retrieval
novelties --- git-history volatility weighting, hybrid
PageRank\,+\,query-conditioned graph attention, session-aware
multi-turn memory with coreference resolution, intent-driven
granularity adaptation, and bidirectional call-context
augmentation --- \chatgit is evaluated on \conv, a 150-conversation
multi-turn benchmark across five Python repositories; primary
evaluation uses 6 curated conversations (18 annotated turns) with
per-turn chunk-level ground truth.

\chatgit achieves \textbf{MRR\,=\,0.349} versus BM25
MRR\,=\,0.116 ($p{=}0.011$, Cohen's $d{=}0.772$, medium-large
effect), and is statistically on par with VanillaRAG
($p{=}0.834$). Session-memory-enabled configurations achieve
\textbf{2.8\%} cross-turn redundancy versus 12.8\% for
VanillaRAG --- 4.6$\times$ lower --- while the naive
ConvAwareRAG baseline \emph{worsens} redundancy to 21.1\%.
Ablation reveals that intent-adaptive retrieval (N4) is the
strongest single component (MRR\,=\,0.444), and that the
per-intent SUMMARIZE fix (exempting class-level chunks from N3
penalties) recovers SUMMARIZE MRR from 0.000 to \textbf{0.500}
across all N4-enabled configurations. On Flask, \chatgit
outperforms VanillaRAG (MRR\,=\,0.756 vs.\ 0.694).
We release the system, the \conv evaluation dataset, and all
evaluation scripts.

\keywords{Conversational Code QA \and Retrieval-Augmented Generation
\and Repository Intelligence \and Session Memory \and Call Graph \and
Intent Classification \and PageRank \and Software Engineering}
\end{abstract}

% ══════════════════════════════════════════════════════════
\section{Introduction}
\label{sec:intro}

Modern software projects routinely span hundreds of files, several
programming languages, nested module hierarchies, and years of
commit history. When a developer joins a project mid-sprint, or
needs to audit an unfamiliar library, the standard tools ---
\texttt{grep}, IDE symbol search, or GitHub's own code search ---
require the user to already know what they are looking for. They
return raw matches, not explanations; they have no memory of what
was asked moments ago; and they treat every file as equally
important regardless of architectural role or change frequency.

Large-language-model (LLM) based coding assistants partially
address the comprehension gap, but three fundamental problems
remain unresolved in current tools:

\textbf{Cross-turn redundancy.} Without retrieval-level session
state, every follow-up question re-retrieves the same
highest-scoring entry-point functions, flooding the context window
with repetitive content and forcing the model to re-explain code
it just explained.

\textbf{Structurally-blind retrieval.} Cosine similarity over
embeddings is content-aware but structurally unaware. It cannot
distinguish a core data-model class --- imported by forty other
files and therefore architecturally authoritative --- from a
one-off utility used nowhere else. It also cannot tell whether a
file has been stable for two years or rewritten twice last week,
even though stability is a meaningful signal for which definition
is trustworthy.

\textbf{Fixed retrieval granularity.} ``Where is
\texttt{authenticate()} defined?'' requires a single tight
function chunk. ``Explain the authentication module'' requires
several file-level summaries. A fixed \texttt{top\_k} and uniform
chunk size serves neither query type well.

\chatgit addresses all three limitations through a pipeline of
complementary technical components. Concretely, our contributions
are:

\begin{enumerate}[label=(\arabic*),leftmargin=*]
  \item A full multi-turn conversational code QA pipeline
        (\S\ref{sec:architecture}--\S\ref{sec:components})
        combining semantic AST chunking, graph-structural retrieval
        signals, session-aware multi-turn memory with coreference
        resolution, intent-adaptive granularity control, and
        post-generation line-number citation injection.
  \item \conv, a 150-conversation multi-turn retrieval benchmark
        across five Python repositories with per-turn chunk-level
        ground truth, intent annotations, and coreference labels.
        Primary evaluation uses 6 curated conversations (18
        annotated turns) (\S\ref{sec:eval}).
  \item An empirical evaluation (\S\ref{sec:results}) demonstrating
        $3.0\times$ MRR improvement over BM25 ($p{=}0.011$,
        $d{=}0.772$, medium-large effect) and statistical parity
        with VanillaRAG ($p{=}0.834$), while achieving 4.6$\times$
        lower cross-turn redundancy; ConvAwareRAG (naive
        augmentation) achieves no such guarantee.
  \item A complete ablation (\S\ref{sec:results}) isolating each
        novelty's contribution: N4 (intent routing) is the strongest
        retrieval component (MRR\,=\,0.444); N3 (session memory)
        reduces redundancy to 1.7\% when combined with N4.
        A targeted per-intent fix (exempting class chunks from N3
        penalties for SUMMARIZE queries) raises SUMMARIZE MRR from
        0.000 to 0.500. The \emph{ConvAwareRAG} baseline shows
        naive query augmentation actively worsens redundancy.
\end{enumerate}

% ══════════════════════════════════════════════════════════
\section{Background and Related Work}
\label{sec:related}

\subsection{Retrieval-Augmented Generation}

RAG~\cite{lewis2021rag} grounds LLM responses by retrieving
relevant passages from an external corpus before generation,
reducing hallucination and enabling knowledge-intensive tasks.
Standard RAG pipelines use a vector store indexed with dense
embeddings and retrieve by cosine similarity~\cite{guo2024lightrag}.
Recent surveys~\cite{petroni2024irrag,abootorabi2025multimodal}
identify multi-hop reasoning and long-document grounding as open
challenges; code repositories present both simultaneously.

\subsection{Code Representation and Retrieval}

BM25~\cite{robertson2009bm25} provides a strong lexical baseline
for code search and remains competitive on identifier-heavy queries.
CodeBERT~\cite{feng2020codebert} and
GraphCodeBERT~\cite{guo2021graphcodebert} extend BERT pre-training
to program tokens and data-flow graphs, producing dense
representations that capture semantic similarity across variable
renames and minor refactors.

RepoCoder~\cite{zhang2023repocoder} proposes iterative
retrieval-generation for code \emph{completion}: given a partial
function, it retrieves similar snippets from the repo and
re-issues retrieval after each generation step. This task
(infilling a known function body) is distinct from our task
(answering open-ended questions about an unknown codebase), and
RepoCoder's design does not address multi-turn session state,
intent adaptation, or cross-turn redundancy.

RepoAgent~\cite{liu2024repoagent} uses an LLM with call-graph
traversal to produce repository-level documentation, but is not
an interactive QA system. SWE-bench~\cite{jimenez2024swebench}
evaluates automated GitHub issue resolution --- a task requiring
code editing, not conversational explanation. Neither addresses
multi-turn dialogue, session coherence, or granularity adaptation.

Benchmarks for code QA include CoSQA~\cite{huang2021cosqa}
(20,604 web queries to code snippets) and
StaQC~\cite{yao2018staqc} (148K Stack Overflow QA pairs).
Both are single-turn and do not evaluate multi-turn conversational
behaviour, session memory, or retrieval from full repositories.
\conv fills this gap with multi-turn, repository-grounded,
intent-annotated conversations.

\subsection{Graph-Based Ranking}

PageRank~\cite{brin1998pagerank} assigns importance scores by
propagating rank through directed edges; it has been applied to
software dependency graphs for bug prediction and change-impact
analysis. HITS~\cite{kleinberg1999hits} decomposes node importance
into hub (links to many authorities) and authority (cited by many
hubs) scores. We combine PageRank and query-conditioned attention
as complementary structural signals, adapting the mixture weight
to query specificity.

\subsection{Multi-Turn and Conversational QA}

CoQA~\cite{reddy2019coqa} and QuAC~\cite{choi2018quac} established
benchmarks for multi-turn QA over flat text passages. Neither
addresses code repositories, call-graph structure, or retrieval
granularity. Coreference resolution in conversational QA has been
studied in open-domain dialogue~\cite{choi2018quac} but not applied
to code-specific retrieval pipelines where the referents are
function names, class hierarchies, and module structures.

\subsection{Commercial AI Coding Assistants}

GitHub Copilot Chat~\cite{githubcopilot}, Sourcegraph
Cody~\cite{cody}, Cursor~\cite{cursor}, Aider~\cite{aider},
and Continue.dev~\cite{continuedev} all offer conversational
interfaces over code. All maintain conversation history within a
session, and several perform some form of codebase indexing
(Cody uses Sourcegraph's code graph; Cursor uses AST-based chunk
embeddings). What none of them expose is
\emph{retrieval-level session state} --- the ability to suppress
chunks already retrieved in prior turns, boost topically-active
files, and expand ambiguous follow-up queries using the session's
function discussion history. Intent-adaptive retrieval parameter
tuning is likewise absent from all surveyed tools.

% ══════════════════════════════════════════════════════════
\section{System Architecture}
\label{sec:architecture}

\chatgit is organised into four pipeline stages
(Figure~\ref{fig:pipeline}).

\textbf{Stage A: Offline Indexing.}
Performed once per repository. The repository is cloned, source
files are parsed with language-specific AST analysers, and the
resulting function and class nodes are converted into token-aware
chunks. Chunks are embedded and stored in a ChromaDB vector index.
Concurrently, a function call graph is built with NetworkX,
and git commit history is analysed to derive file volatility scores.

\textbf{Stage B: Query Pre-Processing.}
For each user turn, the raw query is (i)~classified into one of
four intent categories (LOCATE, EXPLAIN, SUMMARIZE, DEBUG), which
immediately reconfigures retrieval parameters; (ii)~expanded via
coreference resolution using the session's history; and
(iii)~annotated with a session summary that is prepended to the
LLM prompt.

\textbf{Stage C: Retrieval and Rescoring.}
The expanded query is issued against the vector index. Retrieved
candidates are rescored by a chain of structural signals:
hybrid PageRank\,+\,query-conditioned attention, HITS
hub/authority scores, git-volatility weights, and
session-memory adjustments. A cross-encoder reranker with a
per-file diversity cap produces the final ranked list, which is
augmented with bidirectional call-context neighbours.

\textbf{Stage D: Generation.}
The selected chunks, session summary, and caller/callee context
are assembled into an intent-budget prompt and passed to the LLM.
The generated response is post-processed to inject precise
\texttt{file:line} citations, and the session state is updated for
the next turn.

%% ── PIPELINE FIGURE ─────────────────────────────────────
\begin{figure}[t]
\centering
\begin{tikzpicture}[
  sbox/.style 2 args={
    draw=#1!55!black, fill=#1!8, rounded corners=4pt,
    line width=0.75pt, font=\footnotesize,
    text width=#2, align=left, inner sep=7pt,
  },
  arr/.style={-{Stealth[length=5pt,width=4pt]},
              line width=0.9pt, color=#1},
  darr/.style={-{Stealth[length=4pt,width=3pt]},
               line width=0.65pt, dashed, color=#1},
  lbl/.style={font=\scriptsize, text=#1!70!black},
]

\node[sbox={cBlue}{5.3cm}] (A) at (0,0) {%
  \textbf{\textcolor{cBlue!80!black}{A\enspace Offline Indexing}}\\[4pt]
  \textbullet~Clone repository (GitHub URL)\\
  \textbullet~AST parse: function/class nodes\\
  \textbullet~Token-aware chunking + module summaries\\
  \textbullet~BGE-small-en-v1.5 embeddings\\
  \textbullet~ChromaDB vector index\\
  \textbullet~Build call graph (NetworkX)\\
  \textbullet~Git volatility analysis (N1)\\[2pt]
  \textit{one-time per repository}%
};

\node[sbox={cIndigo}{5.3cm}, right=0.6cm of A] (B) {%
  \textbf{\textcolor{cIndigo!80!black}{B\enspace Query Pre-Processing}}\\[4pt]
  \textbullet~Receive user query\\
  \textbullet~Intent classification (N4):\\
  \hspace*{8pt}LOCATE / EXPLAIN / SUMMARIZE / DEBUG\\
  \textbullet~Set $k$, rerank depth, context budget\\
  \textbullet~Coreference resolution (N3, 3 strategies)\\
  \textbullet~Prepend session history summary\\[2pt]
  \textit{online, per query}%
};

\node[sbox={cPurple}{5.3cm}, below=0.7cm of A] (C) {%
  \textbf{\textcolor{cPurple!80!black}{C\enspace Retrieval \& Rescoring}}\\[4pt]
  \textbullet~Cosine-similarity vector retrieval\\
  \textbullet~Hybrid PageRank + QC-attention (N2)\\
  \textbullet~HITS hub/authority boost (by intent)\\
  \textbullet~Git-volatility weight (N1)\\
  \textbullet~Session-memory score adjustment (N3)\\
  \textbullet~Cross-encoder rerank (MiniLM-L-6)\\
  \textbullet~Bidirectional call-context augment. (N5)%
};

\node[sbox={cGreen}{5.3cm}, right=0.6cm of C] (D) {%
  \textbf{\textcolor{cGreen!80!black}{D\enspace Generation}}\\[4pt]
  \textbullet~Assemble intent-budget prompt\\
  \textbullet~Session summary + caller/callee context\\
  \textbullet~Groq Llama-3.1-8b-instant inference\\
  \textbullet~Extract code identifiers from answer\\
  \textbullet~Inject file:line\textsubscript{start}--line\textsubscript{end} citations\\
  \textbullet~Update session memory state (N3)\\[2pt]
  \textit{online, per query}%
};

\draw[arr=cIndigo]
  ([xshift=1.3cm]B.north) -- ++(0,0.5)
  node[above, lbl=cIndigo] {User Query};
\draw[darr=cBlue] (A.south) -- (C.north)
  node[midway, right, lbl=cBlue] {index \&\\[-1pt]signals};
\draw[arr=cIndigo]
  (B.south) to[out=260, in=55] (C.north east);
\draw[arr=cPurple] (C.east) -- (D.west);
\draw[darr=cGreen]
  (D.north) -- ++(0,0.35) -| ([xshift=-0.6cm]B.north)
  node[near start, above, lbl=cGreen] {session update};
\draw[arr=cGreen]
  (D.south) -- ++(0,-0.45)
  node[below, lbl=cGreen] {Answer + file:line citations};
\end{tikzpicture}
\caption{
  \chatgit pipeline. \textbf{Stage A} (blue) runs once when a
  repository is loaded. \textbf{Stages B--D} (indigo, purple, green)
  execute for every user query. Novelty labels (N1--N5) indicate
  which component each stage activates. Dashed arrows show offline
  signals feeding the live retrieval stage.
}
\label{fig:pipeline}
\end{figure}

% ══════════════════════════════════════════════════════════
\section{Technical Components}
\label{sec:components}

\subsection{Token-Aware AST Chunking and Module Summaries}

Language-specific AST parsers (Python \texttt{ast}; regex-based
extractors for JS/TS/Java/C++) identify function and class
boundaries. Each boundary becomes the primary chunk unit,
respecting semantic coherence: a function is never split
mid-body. Token budgets are enforced at MAX\,=\,512 tokens with
an overlap of 64 tokens and a maximum of 6 chunks per top-level
node.

In addition, a \emph{module-summary chunk} is synthesised for each
file by concatenating docstrings and exported symbol signatures
into a compact $\leq$200-token descriptor. These summaries receive
a $1.20\times$ retrieval boost for SUMMARIZE-intent queries (N4);
class-definition chunks receive a $1.40\times$ boost, as they are
the primary ground-truth chunk type for architecture overview
questions. Module-summary chunks are included in the ground-truth
evaluation set for all SUMMARIZE-intent turns (chunk ID format:
\texttt{file.py::\_\_module\_summary\_\_}).

This contrasts with sliding-window approaches (used by BM25-based
baselines) that split at arbitrary token boundaries, discarding
structural context.

\subsection{Git-History Volatility Weighting (N1)}

Not all stable-looking code is equally trustworthy, and not all
actively-developed code is equally relevant. We compute a
three-factor volatility score per file from up to 500 recent
commits:
\begin{equation}
\text{vol}(f) =
  0.5\,\text{freq}(f) + 0.3\,\text{recency}(f) + 0.2\,\text{diversity}(f)
\label{eq:vol}
\end{equation}
where \textit{freq} is the normalised commit count, \textit{recency}
is an exponentially-decayed days-since-last-change score, and
\textit{diversity} is the normalised count of distinct committers.
A stable, foundational file receives a retrieval weight of up to
$1.40\times$ while a file in active flux is weighted at $1.00\times$.

A co-change graph built from commit co-occurrence additionally
captures logical coupling between files that share no import edges
but are consistently modified together, surfacing hidden
dependencies invisible to the call graph.

\textbf{Applicability note.} N1 requires at least a partial commit
history. On shallow-cloned repositories (single commit), all files
receive identical volatility scores and N1 provides no
differentiation. Evaluation on shallow-cloned benchmarks therefore
underestimates N1's effect on production repositories
(\S\ref{sec:disc}).

\subsection{Hybrid PageRank and Query-Conditioned Graph Attention (N2)}

We combine two graph-structural signals:
\begin{equation}
h(v,q) = \alpha(q)\cdot\text{PR}(v)
        +(1{-}\alpha(q))\cdot\text{QCA}(v,q)
\label{eq:hybrid}
\end{equation}
$\text{PR}(v)$ is the standard PageRank score of node $v$ in the
file import graph. $\text{QCA}(v,q)$ is a query-conditioned
neighbourhood attention score:
\[
\text{QCA}(v,q)
  = 0.6\,\text{sim}(e_v, e_q)
  + 0.4\,\textstyle\overline{\text{sim}}_{u\in\mathcal{N}(v)}(e_u, e_q)
\]
where $e_v$ is the node's embedding, $e_q$ is the query embedding,
and $\mathcal{N}(v)$ is the one-hop neighbourhood. The mixture
weight $\alpha(q)\in\{0.25, 0.50, 0.70\}$ adapts to query
specificity: narrow identifier queries weight local embedding
similarity ($\alpha$ low) while broad conceptual questions weight
global rank ($\alpha$ high). The boost is applied multiplicatively:
$\text{score} \mathrel{\times}= 1 + 0.05\,h(v,q)$ when
$h(v,q) > 0.3$.

\subsection{HITS Hub/Authority Scoring}

HITS~\cite{kleinberg1999hits} separates node importance into two
complementary roles via mutual reinforcement:
\begin{equation}
  \text{hub}(v)
    = \textstyle\sum_{u:\,v\to u}\text{auth}(u),\qquad
  \text{auth}(v)
    = \textstyle\sum_{u:\,u\to v}\text{hub}(u)
\label{eq:hits}
\end{equation}
We run HITS on both the file import graph and the function call
graph. \emph{Hub nodes} are orchestrators: entry-point scripts,
CLI commands, and app factories. \emph{Authority nodes} are core
utilities: data models, base classes, and shared helpers.
For SUMMARIZE queries we boost hub scores; for LOCATE and EXPLAIN
queries we boost authority scores.

\subsection{Multi-Turn Session-Aware Retrieval Memory (N3)}

A per-session \texttt{SessionRetrievalMemory} object maintains
state across turns and adjusts candidate scores before reranking:
\begin{equation}
s'(c) = s_0(c)\cdot r(c)\cdot z(c)
\label{eq:session}
\end{equation}
$r(c)$ is a redundancy penalty: $0.30$ if the chunk was retrieved
this turn, $0.60$ if one turn ago, $0.85$ if two or more turns ago.
$z(c) = 1 + 0.30\,w_f$ is a zone-coherence bonus proportional to
the current weight of file $f$ in the session's active-file set;
file weights initialise at $1.0$ on first retrieval and decay by
$0.80^t$ per subsequent turn. Functions explicitly discussed in
prior answers receive a $1.20\times$ boost when they reappear.

Two intent-specific exemptions prevent over-penalisation: (i)
\emph{module-summary chunks} are never subject to the redundancy
penalty --- architecture overview questions legitimately require
the same module summary on every turn; (ii) for SUMMARIZE-intent
queries, \emph{class chunks} are also fully exempt (they are the
actual ground-truth chunk type for architecture overview questions,
e.g.\ the \texttt{Flask}, \texttt{Blueprint}, and
\texttt{BaseCommand} class definitions), while all other
non-summary chunks receive a soft $0.92\times$ penalty instead of
the standard $0.60\times$. Additionally, SUMMARIZE turns are not
recorded in session memory, preventing their broad class retrievals
from penalising those same classes in subsequent EXPLAIN turns.

Three coreference resolution strategies expand ambiguous queries:
\begin{enumerate}[label=(\roman*),leftmargin=*]
  \item \textbf{Temporal back-references}: ``the function I asked
        in the beginning'' is expanded with the first query and the
        first discussed function name.
  \item \textbf{Repeat queries}: ``again?'' or ``more?'' are
        replaced with the previous query text.
  \item \textbf{Pronoun references}: short queries containing
        ``it'', ``this function'', etc.\ are appended with the last
        discussed function name as a context hint.
\end{enumerate}
A rolling session summary (prior questions, recently discussed
functions, active files) is prepended to every LLM prompt, keeping
the model oriented across turns without re-transmitting full
history.

\textbf{N3 vs.\ ConvAwareRAG.} The simplest multi-turn dense
baseline is \emph{ConvAwareRAG}: VanillaRAG with the previous
turn's query appended to the current query before embedding. This
tests whether naive string concatenation accounts for multi-turn
gains. N3's session memory differs in three important ways: (i) it
applies scored \emph{penalties} on already-seen chunks rather than
query expansion, (ii) it maintains \emph{zone coherence bonuses}
that track topical focus across the whole session (not just the
last turn), and (iii) it performs structured coreference
resolution rather than blind concatenation.

\subsection{Intent-Driven Granularity-Adaptive Retrieval (N4)}

A keyword-pattern intent classifier assigns one of four intent
classes and simultaneously sets five retrieval parameters
(Table~\ref{tab:intent_config}).

\begin{table}[t]
\centering
\caption{Intent-specific retrieval configuration.}
\label{tab:intent_config}
\small
\begin{tabular}{lrrrrr}
\toprule
Intent & $k$ & Rerank $n$ & Max/file & Ctx budget & Chunk boost \\
\midrule
LOCATE    & 25 & 6  & 2 & 4\,K  & $1.40\times$ statement \\
EXPLAIN   & 20 & 8  & 3 & 6\,K  & $1.20\times$ function  \\
SUMMARIZE & 12 & 5  & 5 & 7\,K  & $1.40\times$ class\,/\,$1.20\times$ module \\
DEBUG     & 30 & 10 & 4 & 6.5\,K& $1.10\times$ mixed     \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Cross-Encoder Reranking with File-Diversity Capping}

After vector retrieval and graph rescoring, the top-$k$ candidates
are reranked by \texttt{ms-marco-MiniLM-L-6-v2}~\cite{reimers2019sentencebert},
a lightweight cross-encoder trained for passage relevance.
After reranking, a \emph{file-diversity cap} enforces the
\texttt{max\_per\_file} limit from Table~\ref{tab:intent_config},
preventing a single highly-similar file from consuming most of the
context budget.

\subsection{Bidirectional Call-Context Neighbourhood Augmentation (N5)}

A retrieved function chunk without its calling context is
incomplete for explain and debug questions: understanding what a
function does requires knowing \emph{where it is called from} and
\emph{what it calls internally}. For the top-5 seed chunks with
cosine similarity $>0.3$, we retrieve both direct callers and
direct callees from the call graph and augment the candidate pool
with a proportional boost:
\begin{equation}
\text{boost}(c_{\text{nb}})
  \mathrel{+}= 0.06 \times s_{\text{seed}}
\label{eq:callctx}
\end{equation}
Caller chunks are tagged \texttt{[CALLER]} and callee chunks are
tagged \texttt{[CALLEE]} in the prompt. The component is active
for EXPLAIN and DEBUG intents only. The proportional formulation
proved critical: a fixed $+0.05$ boost degraded MRR by $9.7\%$
by injecting low-confidence neighbours at equal weight regardless
of seed quality.

\subsection{Post-Generation Line-Number Enhancement}

After generation, \texttt{ImprovedCodeSnippetExtractor}
post-processes the answer: code identifiers and function names are
extracted, located in the repository index, and
\texttt{file:line\_start--line\_end} annotations are injected
inline. This step is deterministic and requires zero additional
LLM calls, providing navigable citations at no added latency.

% ══════════════════════════════════════════════════════════
\section{Experimental Setup}
\label{sec:eval}

\subsection{ConvCodeBench Dataset Construction}

We introduce \conv, a multi-turn conversational retrieval
benchmark for GitHub repositories. The benchmark is constructed
in two stages.

\textbf{Programmatic generation.} We parse the AST of five
widely-used Python repositories --- Flask, Requests, FastAPI,
Celery, and Click --- and generate templated conversations centred
on individual functions. Each conversation follows a
LOCATE$\to$EXPLAIN$\to$DEBUG pattern (2--4 turns), with the final
turn occasionally a SUMMARIZE query covering the enclosing module.
This produces 150 conversations and 486 turns. Coreference
annotations (pronoun $\to$ referent mappings) are added for
all turns where a follow-up refers implicitly to a prior entity
(111 turns, 22.8\%).

\textbf{Ground-truth chunk IDs.} Each turn is annotated with
one or more expected chunk IDs in the format
\texttt{file.py::function\_name}. For SUMMARIZE turns, the
module-summary chunk (\texttt{file.py::\_\_module\_summary\_\_})
is included as a valid retrieval target alongside the function
chunk, enabling measurement of SUMMARIZE-intent retrieval.

\textbf{Limitations of the dataset.} The programmatic construction
means queries follow predictable templates (``Where is X?'',
``How does X work?''). Ground truth is also template-driven: most
conversations focus on a single function across all turns. This
understates the challenge of true coreference resolution and
topic-shift handling. We discuss this as a limitation in
\S\ref{sec:disc} and encourage future work to supplement with
naturalistic queries written by developers unfamiliar with the
codebase.

\textbf{6-conversation evaluation subset.} We curate 6
conversations (18 turns with chunk-level ground truth) for the
primary evaluation, selected to cover all five repositories and
all four intents, with each conversation containing at least one
coreference turn. These 18 annotated turns are used for all
reported metrics (main table, ablation, per-intent, per-repository).
The full 150-conversation set is released for future benchmarking.

\subsection{Baselines}

\begin{itemize}[leftmargin=*]
  \item \textbf{BM25}: Okapi BM25 ($k_1{=}2.0$, $b{=}0.75$),
        retrieval only, no LLM. Strong lexical baseline for
        identifier-rich code queries.
  \item \textbf{VanillaRAG}: BGE-small-en-v1.5 embeddings,
        cosine top-$k$ retrieval, no graph signals, no session
        state, Llama-3.1-8b generation. Standard off-the-shelf
        RAG pipeline.
  \item \textbf{ConvAwareRAG} [new]: VanillaRAG with the
        previous turn's query appended to the current query before
        embedding. Tests whether naive query augmentation accounts
        for multi-turn gains, used to isolate N3's contribution.
  \item \textbf{BM25-SlidingWindow}: BM25 with one round of
        query augmentation: the top retrieved snippet (first 300
        characters) is appended to the query and BM25 is re-run.
        This tests simple iterative augmentation on a lexical
        retriever. Note: this is \emph{not} RepoCoder
        (Zhang et al., 2023)~\cite{zhang2023repocoder}, which
        is an iterative generation loop for code completion --- a
        different task.
  \item \textbf{\chatgit (Session\,+\,Intent)}: session memory
        and intent-adaptive retrieval without any graph components
        (no PageRank, no HITS, no call-context augmentation).
\end{itemize}

\subsection{Metrics}

\textbf{Retrieval}: MRR (mean reciprocal rank), Recall@5,
NDCG@5, P@1, Success@5, and cross-turn redundancy (fraction of
turns where a chunk in the top-$k$ was also retrieved in a prior
turn of the same conversation).

Statistical significance: Wilcoxon signed-rank test (paired,
non-parametric), bootstrap confidence intervals ($n{=}3{,}000$
resamples), Bonferroni correction for multiple comparisons.

% ══════════════════════════════════════════════════════════
\section{Results}
\label{sec:results}

\subsection{Multi-Turn Retrieval (ConvCodeBench)}

Table~\ref{tab:multiturn} shows results on the 18-turn
multi-turn set (6 conversations). \chatgit (full) achieves MRR\,=\,0.349 versus 0.116
for BM25 ($p{=}0.011$, Cohen's $d{=}0.772$, medium-large effect),
and is statistically on par with VanillaRAG (0.375, $p{=}0.834$).
Cross-turn redundancy for N3-enabled configurations is 1.7--2.8\%
versus 12.8\% for VanillaRAG, a 4.6--7.5$\times$ reduction.
Intent routing alone (N4, MRR\,=\,0.444) is the strongest
single retrieval component and exceeds VanillaRAG by $+0.069$.

\begin{table}[t]
\centering
\caption{Multi-turn retrieval on \conv (6 conversations, 18 annotated
turns, 5 repos). \textbf{Bold}: best per column.
$^{*}p{<}0.05$ vs.\ BM25 (Wilcoxon signed-rank).
CI: bootstrap 95\% ($n{=}3{,}000$).}
\label{tab:multiturn}
\small
\begin{tabular}{lccccc}
\toprule
System & MRR & R@5 & NDCG@5 & P@1 & Redund. \\
\midrule
\rowcolor{gray!8}
BM25
  & 0.116$_{\pm.078}$ & 0.194 & 0.111 & 0.000 & 10.0\% \\
BM25-SlidingWindow
  & 0.125$_{\pm.075}$ & 0.194 & 0.111 & 0.000 & 10.0\% \\
VanillaRAG
  & 0.375$_{\pm.189}$ & 0.278 & 0.266 & 0.278 & 12.8\% \\
ConvAwareRAG
  & 0.246$_{\pm.171}$ & 0.296 & 0.242 & 0.167 & 21.1\% \\
\chatgit (N4 only)
  & \textbf{0.444}$_{\pm.205}$ & 0.389 & \textbf{0.345} & \textbf{0.389} & 12.8\% \\
\chatgit (N3+N4)
  & 0.356$_{\pm.191}$ & 0.389 & 0.315 & 0.278 & 1.7\% \\
\rowcolor{cIndigo!8}
\textbf{\chatgit (full)}
  & 0.349$_{\pm.177}$
  & \textbf{0.389} & 0.302 & 0.222
  & 2.8\% \\
\midrule
\multicolumn{6}{l}{\small vs.\ BM25: $\Delta{=}{+}0.233$,
  $p{=}0.011^{*}$, $d{=}0.772$ (medium-large effect)} \\
\multicolumn{6}{l}{\small vs.\ VanillaRAG: $\Delta{=}{-}0.026$,
  $p{=}0.834$ (n.s.; statistical parity — see \S\ref{sec:disc})} \\
\multicolumn{6}{l}{\small ConvAwareRAG redundancy 21.1\% $>$
  VanillaRAG 12.8\% (naive augment.\ worsens repetition)} \\
\bottomrule
\end{tabular}
\end{table}

\textbf{ConvAwareRAG gap.}
ConvAwareRAG (MRR\,=\,0.246) \emph{decreases} by $-0.129$ MRR
relative to VanillaRAG (0.375), and its redundancy rate
(21.1\%) is \emph{worse} than both VanillaRAG (12.8\%) and BM25
(10.0\%). This counter-intuitive result reveals why naive query
augmentation is not a valid multi-turn strategy: appending the
previous query re-biases the query embedding toward recently-seen
content, \emph{increasing} the probability of retrieving the same
chunks again. N3's structured scoring (score penalties on seen
chunks, zone-coherence bonuses for topical exploration) avoids
this failure: \chatgit (N3+N4) achieves 1.7\% redundancy while
maintaining Recall@5\,=\,0.389 and Success@10\,=\,0.611.

\subsection{Ablation Study}

Table~\ref{tab:ablation} traces the incremental contribution of
each novelty from the Vanilla base. The ablation uses an
\emph{additive} design: each row adds one component to the
configuration above it, enabling isolation of each novelty's
marginal contribution.

\begin{table}[t]
\centering
\caption{Incremental ablation on \conv (18 turns, 6 conversations).
Each row adds one novelty over the previous configuration.
$\dagger$: shallow-clone repos suppress N1/N2 differentiation
($\leq$50 commits); see \S\ref{sec:disc}.
Redundancy: cross-turn chunk repetition rate.}
\label{tab:ablation}
\small
\begin{tabular}{lccccc}
\toprule
Configuration & MRR & R@5 & NDCG@5 & $\Delta$MRR & Redund. \\
\midrule
Vanilla (no novelties)  & 0.375 & 0.278 & 0.266 & --- & 12.8\% \\
\quad $+$N3 only (session mem.) & 0.224 & 0.296 & 0.222 & $-0.151$ & \textbf{0.6\%} \\
\quad $+$N4 only (intent rout.) & \textbf{0.444} & 0.389 & \textbf{0.345} & $+0.069$ & 12.8\% \\
$+$N3\,+\,N4                   & 0.356 & 0.389 & 0.315 & $-0.019$ & 1.7\% \\
\quad $+$N1 (volatility)$^{\dagger}$ & 0.356 & 0.389 & 0.315 & $\pm$0.000 & 1.7\% \\
\quad $+$N2 (hybrid PR)$^{\dagger}$ & 0.342 & 0.389 & 0.302 & $-0.014$ & 1.7\% \\
\quad $+$N5 (call-context)     & 0.363 & 0.389 & 0.315 & $+0.021$ & 2.8\% \\
\chatgit (full, all N1--N5)    & 0.349 & \textbf{0.389} & 0.302 & $-0.014$ & 2.8\% \\
\bottomrule
\end{tabular}
\end{table}

The most striking finding is the \textbf{N4 dominance}: intent
routing alone raises MRR by $+0.069$ over Vanilla (0.444 vs.\
0.375), the largest single-component gain. The granularity boost
routes EXPLAIN queries to function-level chunks, LOCATE queries
to tight function signatures, and --- critically --- boosts class
chunks by $1.40\times$ for SUMMARIZE queries, correctly
targeting the class-definition ground truth for architecture
overview questions.

\textbf{N3 alone} reduces MRR by $-0.151$ (0.224 vs.\ 0.375),
as expected: redundancy penalties suppress already-seen chunks
even when they are highest-scoring, a deliberate design choice.
But N3 reduces redundancy to 0.6\% (vs.\ 12.8\% baseline).

\textbf{N3+N4 combined} nearly matches VanillaRAG MRR
(0.356 vs.\ 0.375, $\Delta{=}{-}0.019$) with only 1.7\%
redundancy. The N4 class-boost fix for SUMMARIZE turns prevents
these broad retrievals from polluting session memory for
subsequent EXPLAIN turns, so the two components now cooperate
rather than conflict.

N5 (call-context) contributes $+0.021$ MRR over the N3+N4+N2 base.
N1 and N2 are neutral-to-slightly-negative on this shallow-clone
benchmark ($\pm$0.000 and $-0.014$ respectively); both require
multi-commit history to provide useful differentiation between
files (\S\ref{sec:disc}).

\subsection{Per-Intent and Per-Repository Analysis}

Table~\ref{tab:per_intent} shows per-intent MRR. N4 (intent
routing) drives strong gains on EXPLAIN (VanillaRAG 0.370
$\to$ N4-alone 0.473) and SUMMARIZE (VanillaRAG 0.250 $\to$
N4-alone 0.500). LOCATE reaches 0.500 under both VanillaRAG and
N4-alone, hitting a ceiling on this small set.

SUMMARIZE recovers from 0.000 in earlier configurations to
\textbf{0.500} in all N4-enabled configurations. The fix
operates at two levels: (i)~N4 boosts class chunks by
$1.40\times$ for SUMMARIZE queries, correctly targeting class
definitions (Flask, Blueprint, BaseCommand) that are the
chunk-level ground truth for architecture overview questions;
(ii)~N3 exempts class chunks from redundancy penalties when
intent\,=\,summarize, and does not record SUMMARIZE turns in
session memory, preventing broad class retrievals from polluting
session state for subsequent focused EXPLAIN turns.

\begin{table}[t]
\centering
\caption{Per-intent MRR (18 turns). N4-only is best by single-turn
MRR; N3+N4 achieves 1.7\% redundancy (vs.\ 12.8\% Vanilla)
at small MRR cost (see \S\ref{sec:disc}).}
\label{tab:per_intent}
\small
\begin{tabular}{lcccc}
\toprule
System & LOCATE & EXPLAIN & DEBUG & SUMMARIZE \\
\midrule
BM25              & 0.198 & 0.042 & 0.208 & 0.063 \\
VanillaRAG        & 0.500 & 0.370 & 0.323 & 0.250 \\
\chatgit (N4 only) & \textbf{0.500} & \textbf{0.473}
                  & 0.300 & \textbf{0.500} \\
\chatgit (N3+N4)  & \textbf{0.500} & 0.321 & 0.208 & \textbf{0.500} \\
\chatgit (full)   & 0.375 & 0.337 & \textbf{0.271} & \textbf{0.500} \\
$\Delta$ (full vs VanillaRAG)
                  & $-$0.125 & $-$0.033 & $-$0.052 & $+$0.250 \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:per_repo} shows per-repository MRR. Flask shows the
most dramatic improvement: \chatgit achieves MRR\,=\,0.756,
\emph{outperforming} VanillaRAG (0.694) by $+0.062$. The key
driver is the SUMMARIZE fix --- Flask's ground truth for
architecture-overview turns is the \texttt{Flask} class definition,
and the N4 class-boost (1.40$\times$ for SUMMARIZE) combined with
N3's class-exemption correctly retrieves it. Requests also shows
strong semantic gains over BM25 ($+0.055$ over VanillaRAG 0.375).
FastAPI and Celery show modest results; both are shallow-cloned
(50 commits), suppressing N1 and N2 signals.

\textbf{Click failure analysis.} Click performs below BM25
(MRR\,=\,0.056 vs.\ 0.111). Analysis of the Click test set reveals
two compounding issues: (i)~Click's flat argument-parser design
means most functions are leaves in the call graph with no callers
or callees in the static graph, eliminating N5's contribution;
(ii)~the Click conversation in our evaluation subset targets
utility functions in \texttt{types.py} and \texttt{core.py} whose names are common English words
(\texttt{convert}, \texttt{resolve}, \texttt{pop\_context}),
causing high-similarity false positives in dense retrieval.
BM25's identifier-matching advantage on these short, distinctive
names compensates for its general weakness on semantic queries.
This finding scopes \chatgit's advantages to architecturally
layered codebases with meaningful call graphs; flat/imperative
CLI codebases may be better served by tuned lexical search.

\begin{table}[t]
\centering
\caption{Per-repository MRR, full \chatgit vs.\ VanillaRAG and BM25.
$\Delta$: \chatgit minus VanillaRAG.}
\label{tab:per_repo}
\small
\begin{tabular}{lcccc}
\toprule
Repository & \chatgit (full) & VanillaRAG & BM25 & $\Delta$ (vs VanillaRAG) \\
\midrule
flask    & \textbf{0.756}  & 0.694 & 0.132 & $+$0.062 \\
requests & 0.333           & \textbf{0.375} & 0.278 & $-$0.042 \\
fastapi  & \textbf{0.108}  & 0.097 & 0.000 & $+$0.011 \\
celery   & 0.083           & \textbf{0.333} & 0.042 & $-$0.250 \\
click    & 0.056           & 0.056 & \textbf{0.111} & $\pm$0.000 \\
\midrule
\multicolumn{5}{l}{\small Wilcoxon vs.\ BM25: $p{=}0.011$, $d{=}0.772$;
  vs.\ VanillaRAG: $p{=}0.834$ (n.s.)} \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Latency and Efficiency}
\label{sec:latency}

Table~\ref{tab:latency} reports indexing and retrieval latency
across three repository sizes (small: flask, 578 chunks; medium:
requests, 384 chunks; large: fastapi, 2{,}567 chunks),
benchmarked on a Windows~11 machine with an NVIDIA GPU
(BGE-small-en-v1.5 embedding model, batch size~256).

\begin{table}[t]
\centering
\caption{Indexing and per-query retrieval latency. ChatGIT adds
intent classification and session-memory scoring on top of
VanillaRAG. All retrieval figures are mean over $n{=}50$ queries
(5 warmup, 50 measured).}
\label{tab:latency}
\small
\begin{tabular}{llccrr}
\toprule
\multicolumn{2}{l}{} & \multicolumn{2}{c}{Indexing} &
\multicolumn{2}{c}{Retrieval (mean ms)} \\
\cmidrule(lr){3-4}\cmidrule(lr){5-6}
Repo & Size & Chunks & Total (s) & VanillaRAG & ChatGIT \\
\midrule
flask   & small  &   578 & 26.5 & 22.3 & 24.8 \\
requests& medium &   384 & 17.0 & 21.9 & 24.5 \\
fastapi & large  & 2\,567 & 74.5 & 23.9 & 32.5 \\
\midrule
\multicolumn{6}{l}{\small BM25 retrieval: 3--19\,ms (lexical only, no embedding).
  Index memory: 0.56--3.76\,MB.} \\
\bottomrule
\end{tabular}
\end{table}

\chatgit adds ${\sim}$2--9\,ms latency over VanillaRAG (intent
classification + session-memory scoring). On small and medium
repos this overhead is under 3\,ms (${\leq}$12\% relative);
on the large fastapi repo the overhead grows to 8.6\,ms (36\%
relative) as session-memory scoring iterates over 2{,}567 chunks.
All three systems comfortably exceed interactive-response
requirements ($<$50\,ms retrieval latency). Index memory is
0.56--3.76\,MB (float32 embeddings), well within laptop RAM.
Indexing is dominated by embedding time (97\%+); chunking is
under 4\,seconds even for the largest repo.

% ══════════════════════════════════════════════════════════
\section{Discussion}
\label{sec:disc}

\paragraph{N4 maximises MRR; N3+N4 achieves statistical parity with VanillaRAG.}
The ablation reveals a \emph{complementary} relationship between
N3 and N4 when properly tuned. N4 (intent routing) is the
strongest single-turn retrieval component (MRR\,=\,0.444),
exceeding VanillaRAG by $+0.069$. The class-chunk boost
($1.40\times$ for SUMMARIZE) directly targets the ground-truth
chunk type for architecture overview queries. N3 (session memory)
adds redundancy control: penalties on already-seen chunks reduce
repetition from 12.8\% to 1.7\%. Combined N3+N4 achieves
MRR\,=\,0.356 ($\Delta{=}{-}0.019$ vs.\ Vanilla) --- effectively
matching VanillaRAG while providing 7.5$\times$ lower redundancy.
The key design insight is that SUMMARIZE turns must not be
recorded in session memory: their broad class retrievals would
otherwise penalise those same classes in subsequent EXPLAIN turns.

\paragraph{N3 vs.\ ConvAwareRAG: penalties not concatenation.}
ConvAwareRAG achieves MRR\,=\,0.246 --- lower than VanillaRAG
(0.375) --- with 21.1\% redundancy. The naive
previous-query-concatenation strategy \emph{increases} retrieval
repetition by re-biasing the query embedding toward recently-seen
content. N3's explicit chunk-level penalty scoring reduces this to 1.7\%
redundancy for N3+N4 — a 7.5$\times$ reduction over Vanilla.

\paragraph{N1 and N2 require full commit history.}
Both signals require a meaningful commit graph. Shallow-cloned
test repositories with only a single commit provide no per-file
frequency or recency variance; every file receives an identical
volatility score. Call graphs derived from shallow clones may
also miss import edges from files not present in the working tree.
On full-history repositories, these signals are expected to
differentiate architectural layers more strongly: files with high
commit frequency and author diversity (actively contested code)
versus files stable for years (foundational, well-understood code)
represent qualitatively different retrieval targets. The negative
effect ($-0.007$) observed in our shallow-clone evaluation is
an artefact of the benchmark construction, not evidence that
the signal is harmful.

\paragraph{HITS separates roles that PageRank conflates.}
PageRank assigns a single global centrality score that rewards
files with many in-links. HITS separates orchestrators (high
out-relevance, hub score) from core utilities (high in-relevance,
authority score), enabling intent-specific boosting. On repositories
with clear architectural layering (FastAPI, Flask), routing
SUMMARIZE queries to hub nodes and LOCATE/EXPLAIN queries to
authority nodes produces more coherent retrieved sets.

\paragraph{VanillaRAG as a strong retrieval baseline.}
VanillaRAG achieves MRR\,=\,0.375. \chatgit (full) achieves
0.349 ($p{=}0.834$, n.s.) --- statistically on par while
providing 4.6$\times$ lower cross-turn redundancy (2.8\% vs.\
12.8\%) and SUMMARIZE gains (0.500 vs.\ 0.250). On Flask,
\chatgit outperforms VanillaRAG (0.756 vs.\ 0.694). The remaining
gap on Celery ($-0.250$) reflects the shallow-clone limitation
(50 commits, suppressing N1/N2 signals) and Celery's distributed
architecture where core task-dispatch classes span many files,
making single-chunk retrieval inherently harder. Whether MRR or
redundancy rate better predicts user satisfaction in an
interactive code assistant is an empirical question that
motivates the human evaluation planned for future work.

% ══════════════════════════════════════════════════════════
\section{Limitations}
\label{sec:limitations}

\begin{enumerate}[leftmargin=*,label=(\arabic*)]
  \item \textbf{Small evaluation set.} The primary multi-turn
        results rest on 6 conversations (18 annotated turns).
        Bootstrap CIs are $\pm$0.18--0.22 MRR; the VanillaRAG
        comparison is not significant ($p{=}0.834$). Running the
        full 150-conversation benchmark with annotated ground truth
        is expected to tighten these estimates; the dataset is
        released for this purpose.

  \item \textbf{Programmatic dataset.} \conv conversations follow
        predictable templates and focus on a single function across
        most turns. Real developer conversations involve topic
        shifts, cross-file reasoning, and naturalistic phrasing
        not captured here.

  \item \textbf{N3 intent specificity.} N3 redundancy penalties
        are now tuned per intent (class chunks exempt for SUMMARIZE;
        SUMMARIZE turns not recorded in session memory). The
        remaining per-intent gap (N3+N4 DEBUG MRR\,=\,0.208 vs.\
        VanillaRAG 0.323) suggests further per-intent penalty
        calibration is possible.

  \item \textbf{N1/N2 shallow-clone limitation.} As discussed in
        \S\ref{sec:disc}, the shallow-clone test environment
        suppresses these signals. Evaluation on full-history
        repositories remains future work.

  \item \textbf{Keyword-based intent classification.} The current
        classifier is a regex/keyword pattern matcher. Ambiguous
        queries (``show me the auth code'') may be misclassified.
        A trained neural classifier is expected to improve both
        precision and recall on hybrid intents.

  \item \textbf{Python-only evaluation.} All evaluated repositories
        are Python. The system supports JS/TS/Java/C++ via
        regex-based AST extraction, but retrieval performance on
        these languages has not been independently validated.

  \item \textbf{No generation quality evaluation.} We evaluate
        \emph{retrieval} quality through chunk-level MRR and
        Recall@5. End-to-end answer quality (CodeBLEU, ROUGE-L,
        human Likert ratings) is left for future work; the
        evaluation framework (\texttt{evaluation/eval\_generation.py},
        \texttt{evaluation/human\_eval\_protocol.py}) is fully
        implemented and can be run with live Groq API calls.
\end{enumerate}

% ══════════════════════════════════════════════════════════
\section{Conclusion}
\label{sec:conc}

We presented \chatgit, a multi-turn conversational code QA system
addressing cross-turn redundancy, structurally-blind retrieval,
and fixed granularity. On the \conv benchmark, \chatgit
significantly outperforms BM25 (\textbf{MRR\,=\,0.349 vs.\
0.116}, $p{=}0.011$, $d{=}0.772$, medium-large effect) and
achieves statistical parity with VanillaRAG ($p{=}0.834$) while
providing 4.6$\times$ lower cross-turn redundancy. Intent routing
(N4) is the strongest single retrieval component
(MRR\,=\,0.444, exceeding VanillaRAG by $+0.069$); a targeted
per-intent fix raises SUMMARIZE MRR from 0.000 to \textbf{0.500}.
On Flask, \chatgit outperforms VanillaRAG (0.756 vs.\ 0.694).
The ConvAwareRAG comparison reveals that naive query augmentation
is counter-productive: it increases redundancy to 21.1\% while
reducing MRR below VanillaRAG.

The primary open tasks are: (i)~human evaluation to measure
whether lower redundancy improves user experience relative to
higher-MRR but repetitive configurations; (ii)~evaluating N1
and N2 on full-history repositories (100+ commits);
(iii)~further per-intent penalty calibration for DEBUG and EXPLAIN
intents; and (iv)~replacing keyword-based intent classification
with a trained classifier.

\paragraph{Acknowledgements.}
This project was implemented by Anish Gupta, Prakhar Sethi, and
Ritwik Bhattacharya under the supervision of Dr.~Sonia Khetarpaul.
We thank Dr.~Khetarpaul for her guidance throughout.

% ══════════════════════════════════════════════════════════
\begin{thebibliography}{28}

\bibitem{githubcopilot}
GitHub: GitHub Copilot Chat.
\url{https://docs.github.com/en/copilot/github-copilot-chat} (2024)

\bibitem{cody}
Sourcegraph: Cody --- AI Coding Assistant.
\url{https://sourcegraph.com/cody} (2024)

\bibitem{cursor}
Anysphere Inc.: Cursor --- The AI Code Editor.
\url{https://www.cursor.com} (2024)

\bibitem{aider}
Gauthier, P.: Aider: AI Pair Programming in your Terminal.
\url{https://aider.chat} (2024)

\bibitem{continuedev}
Continue.dev: Open-Source Autopilot for Software Development.
\url{https://continue.dev} (2024)

\bibitem{lewis2021rag}
Lewis, P., et~al.: Retrieval-Augmented Generation for
Knowledge-Intensive NLP Tasks. arXiv:2005.11401 (2021)

\bibitem{petroni2024irrag}
Petroni, F., Siciliano, F., Silvestri, F., Trappolini, G.:
IR-RAG @ SIGIR24: Information Retrieval's Role in RAG Systems.
In: Proc.\ 47th ACM SIGIR, pp.\ 3036--3039 (2024)

\bibitem{guo2024lightrag}
Guo, Z., et~al.: LightRAG: Simple and Fast
Retrieval-Augmented Generation. arXiv:2410.05779 (2025)

\bibitem{robertson2009bm25}
Robertson, S., Zaragoza, H.: The Probabilistic Relevance
Framework: BM25 and Beyond.
Found.\ Trends IR \textbf{3}(4), 333--389 (2009)

\bibitem{feng2020codebert}
Feng, Z., et~al.: CodeBERT: A Pre-Trained Model for Programming
and Natural Languages. In: Findings of EMNLP (2020)

\bibitem{guo2021graphcodebert}
Guo, D., et~al.: GraphCodeBERT: Pre-Training Code Representations
with Data Flow. In: ICLR (2021)

\bibitem{zhang2023repocoder}
Zhang, F., et~al.: RepoCoder: Repository-Level Code Completion
Through Iterative Retrieval and Generation. In: NeurIPS (2023)

\bibitem{liu2024repoagent}
Liu, B., et~al.: RepoAgent: An LLM-Powered Framework for
Repository-level Code Documentation Generation.
arXiv:2402.16667 (2024)

\bibitem{kleinberg1999hits}
Kleinberg, J.M.: Authoritative Sources in a Hyperlinked
Environment. J.\ ACM \textbf{46}(5), 604--632 (1999)

\bibitem{brin1998pagerank}
Brin, S., Page, L.: The Anatomy of a Large-Scale Hypertextual
Web Search Engine. Comput.\ Netw.\ ISDN Syst. (1998)

\bibitem{velickovic2018gat}
Veli\v{c}kovi\'{c}, P., et~al.: Graph Attention Networks.
In: ICLR (2018)

\bibitem{reimers2019sentencebert}
Reimers, N., Gurevych, I.: Sentence-BERT: Sentence Embeddings
using Siamese BERT-Networks. In: EMNLP (2019)

\bibitem{nogueira2019passage}
Nogueira, R., Cho, K.: Passage Re-ranking with BERT.
arXiv:1901.04085 (2019)

\bibitem{reddy2019coqa}
Reddy, S., et~al.: CoQA: A Conversational Question Answering
Challenge. TACL \textbf{7}, 249--266 (2019)

\bibitem{choi2018quac}
Choi, E., et~al.: QuAC: Question Answering in Context.
In: EMNLP (2018)

\bibitem{jimenez2024swebench}
Jimenez, C.E., et~al.: SWE-bench: Can Language Models Resolve
Real-World GitHub Issues? In: ICLR (2024)

\bibitem{bavota2015changeadvisor}
Bavota, G., et~al.: How the Apache Community Upgrades
Dependencies. In: MSR (2015)

\bibitem{hindi2025rag}
Hindi, M., et~al.: Enhancing Precision and Interpretability of
RAG in Legal Technology. IEEE Access (2025)

\bibitem{abootorabi2025multimodal}
Abootorabi, M.M., et~al.: Ask in Any Modality: A Comprehensive
Survey on Multimodal RAG. arXiv:2502.08826 (2025)

\bibitem{huang2021cosqa}
Huang, J., et~al.: CoSQA: 20,000+ Web Queries for Code Search
and Question Answering. In: ACL (2021)

\bibitem{yao2018staqc}
Yao, Z., et~al.: StaQC: A Systematically Mined
Question-Code Dataset from Stack Overflow. In: WWW (2018)

\bibitem{nogueira2020t5}
Nogueira, R., et~al.: Document Ranking with a Pretrained
Sequence-to-Sequence Model. In: Findings of EMNLP (2020)

\end{thebibliography}

\end{document}
